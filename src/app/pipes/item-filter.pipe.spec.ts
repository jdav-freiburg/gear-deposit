/* tslint:disable:no-unused-variable */

import { ItemFilterPipe } from './item-filter.pipe';

describe('Pipe: ItemFilter', () => {
    it('create an instance', () => {
        let pipe = new ItemFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
